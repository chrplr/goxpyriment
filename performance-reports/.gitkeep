Reports generated by tests/Timing-Tests live here, one directory per machine.

Empty because the earlier reports were removed on 2026-08-07: they predate
goxpyriment requesting real-time scheduling at startup and the correction to the
DLP-IO8 driver's Send, either of which changes what they measured. Recover them
from git history if a before/after comparison is wanted.
