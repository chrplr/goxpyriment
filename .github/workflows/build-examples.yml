name: Build example installers

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

jobs:
  build-all:
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: 0
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Install FUSE (for appimagetool)
        run: |
          sudo apt-get update
          sudo apt-get install -y libfuse2

      - name: Download appimagetool
        run: |
          mkdir -p examples/installers/AppImages
          wget -q https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage \
            -O examples/installers/appimagetool
          chmod +x examples/installers/appimagetool

      - name: Build all platforms
        run: bash examples/installers/build-all-platforms.sh

      - name: Upload Windows zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-zip
          path: examples/installers/goxpyriment-examples-windows-x86_64.zip

      - name: Upload macOS arm64 zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-zip
          path: examples/installers/goxpyriment-examples-macos-arm64.zip

      - name: Upload Linux AppImages artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-appimages
          path: examples/installers/goxpyriment-examples-linux-x86_64-appimages.tar.gz

  create-release:
    runs-on: ubuntu-latest
    needs: [build-all]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum \
            goxpyriment-examples-windows-x86_64.zip \
            goxpyriment-examples-macos-arm64.zip \
            goxpyriment-examples-linux-x86_64-appimages.tar.gz \
            > SHA256SUMS

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Goxpyriment examples ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          files: |
            artifacts/goxpyriment-examples-windows-x86_64.zip
            artifacts/goxpyriment-examples-macos-arm64.zip
            artifacts/goxpyriment-examples-linux-x86_64-appimages.tar.gz
            artifacts/SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
